# Hardhat Token Project

This project demonstrates a basic Token and vesting use case. It comes with  contract, a test for that contract.

Try running some of the following tasks:

```shell
npx hardhat help
npx hardhat test
REPORT_GAS=true npx hardhat test
npx hardhat node
npx hardhat ignition deploy ./ignition/modules/Lock.js
```
